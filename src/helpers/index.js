import data from "./data.js"

import {
    getTimeAgo,
    toFirstLetterUpperCase,
    markdownToHTML,
} from "./javascriptFunctions"

import {
    numberOfArticlesPerPage,
} from "./constants"

export {
    data,
    getTimeAgo,
    toFirstLetterUpperCase,
    numberOfArticlesPerPage,
    markdownToHTML,
}